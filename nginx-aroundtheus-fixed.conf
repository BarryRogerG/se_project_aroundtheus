server {
    listen 80;
    server_name 34.134.30.14 aroundtheus.bestbudwtwr.ignorelist.com;

    root /var/www;

    # Explicitly serve main.js - must come before other location blocks
    location = /aroundtheus/main.js {
        try_files $uri =404;
        add_header Content-Type "application/javascript";
    }

    # Explicitly serve main.css
    location = /aroundtheus/main.css {
        try_files $uri =404;
        add_header Content-Type "text/css";
    }

    # Serve all other static files in aroundtheus
    location ~ ^/aroundtheus/.+\.(png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        try_files $uri =404;
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # Handle directory requests for aroundtheus
    location /aroundtheus/ {
        try_files $uri $uri/ /aroundtheus/index.html;
    }

    # Redirect /aroundtheus to /aroundtheus/
    location = /aroundtheus {
        return 301 /aroundtheus/;
    }
}

